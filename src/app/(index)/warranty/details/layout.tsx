export const metadata = {
    title: 'Warranty Details',
    description: 'Warranty Details',
}

export default function WarrantyDetailsLayout({ children }: { children: React.ReactNode }) {
    return (
        <>
            {children}
        </>
    )
}